[project]
name = "cloud-platform-application"
version = "0.1.0"
description = "A Flask API used to build and learn a cloud-native platform"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
]
